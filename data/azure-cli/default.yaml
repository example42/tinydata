---
azure-cli::settings:
  package_name: azure-cli
  version_command: 'az -v'
  docker_image: 'bitnami/azure-cli'
  tp_prerequisites:
    - microsoft_repo

# Version 4 format
  packages:
    main:
      name: azure-cli
  image:
    name: bitnami/azure-cli
  preinstall:
    tp::install:
    - microsoft_repo
