---
bind::settings:
  pid_file_path: '/var/run/named/named.pid'
  log_file_path: '/var/log/syslog'
  service_name: 'bind9'
  process_name: 'named'
  process_user: 'bind'
  process_group: 'bind'

# Version 4 format
  services:
    bind9:
      process_name: named
      process_user: bind
      process_group: bind
  files:
    log:
      path: "/var/log/syslog"
    pid:
      path: "/var/run/named/named.pid"
