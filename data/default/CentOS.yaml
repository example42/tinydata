---
default::settings:
  repo_package_provider: 'rpm'
  init_system: 'systemd'

  repos:
    default:
      package_provider: 'rpm'

# Version 4 format
  repo:
    upstream:
      package_provider: rpm
