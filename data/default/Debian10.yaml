---
default::settings:
  apt_release: 'buster'
  init_system: 'systemd'

  repos:
    default:
      apt:
        release: 'buster'

# Version 4 format
  repo:
    upstream:
      apt:
        release: buster
