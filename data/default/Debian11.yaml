---
default::settings:
  apt_release: 'bullseye'
  init_system: 'systemd'

  repos:
    default:
      apt:
        release: 'bullseye'

# Version 4 format
  repo:
    upstream:
      apt:
        release: bullseye
