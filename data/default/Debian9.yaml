---
default::settings:
  apt_release: 'stretch'
  init_system: 'systemd'

  repos:
    default:
      apt:
        release: 'stretch'

# Version 4 format
  repo:
    upstream:
      apt:
        release: stretch
