---
default::settings:
  apt_release: 'chimaera'
  init_system: 'sysvinit'
  service_provider: 'sysvinit'

  repos:
    default:
      apt:
        release: 'chimaera'

# Version 4 format
  repo:
    upstream:
      apt:
        release: chimaera
