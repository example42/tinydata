---
default::settings:
  repo_package_provider: dpkg
  init_system: 'systemd'

  repos:
    default:
      package_provider: dpkg

# Version 4 format
  repo:
    upstream:
      package_provider: dpkg
