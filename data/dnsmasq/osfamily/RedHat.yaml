---
dnsmasq::settings:
  process_user: 'nobody'
  init_file_path: '/etc/sysconfig/dnsmasq'

# Version 4 format
  services:
    ! '':
      process_user: nobody
  files:
    init:
      path: "/etc/sysconfig/dnsmasq"
