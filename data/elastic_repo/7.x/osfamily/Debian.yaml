---
elastic_repo::settings:
  repo_url: 'http://artifacts.elastic.co/packages/7.x/apt'
  apt_repos: 'main'
  apt_release: 'stable'

# Version 4 format
  repo:
    upstream:
      url: http://artifacts.elastic.co/packages/7.x/apt
      apt:
        release: stable
