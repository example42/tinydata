---
git::settings:
  winget_package_name: 'Git.Git'
  choco_package_name: 'git'
  user_config_dir: '%HOMEPATH%/.git'
  user_config_path: '%HOMEPATH%/.gitconfig'

# Version 4 format
  packages:
    main:
      providers:
        winget:
          name: Git.Git
        choco:
          name: git
