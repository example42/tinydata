---
glance::settings:
  package_name: 'glance'
  init_file_path: '/etc/default/glance'

# Version 4 format
  packages:
    main:
      name: glance
  files:
    init:
      path: "/etc/default/glance"
