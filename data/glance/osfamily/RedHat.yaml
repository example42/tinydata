---
glance::settings:
  package_name: 'openstack-glance'
  init_file_path: '/etc/sysconfig/glance'


# Version 4 format
  packages:
    main:
      name: openstack-glance
  files:
    init:
      path: "/etc/sysconfig/glance"
