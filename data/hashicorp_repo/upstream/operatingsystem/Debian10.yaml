---
hashicorp_repo::settings:
  repo_url: 'https://apt.releases.hashicorp.com'
  apt_release: 'buster main'

# Version 4 format
  repo:
    upstream:
      url: https://apt.releases.hashicorp.com
      apt:
        release: buster main
