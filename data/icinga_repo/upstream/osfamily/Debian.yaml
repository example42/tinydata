---
icinga_repo::settings:
  repo_url: 'https://packages.icinga.com/debian'
  apt_repos: 'main'
  apt_release: 'stable'

# Version 4 format
  repo:
    upstream:
      url: https://packages.icinga.com/debian
      apt:
        release: stable
