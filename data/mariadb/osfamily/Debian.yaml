---
mariadb::settings:
  package_name: 'mariadb-server'
  init_file_path: '/etc/default/mysql'

# Version 4 format
  packages:
    main:
      name: mariadb-server
  files:
    init:
      path: "/etc/default/mysql"
