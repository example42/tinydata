---
mongodb-mms-monitoring-agent::settings:
  package_name: 'mongodb-mms-monitoring-agent'
  service_name: 'mongodb-mms-monitoring-agent'
  config_file_path: '/etc/mongodb-mms/monitoring-agent.config'
  pid_file_path: '/var/run/mongodb-mms-monitoring-agent.pid'
  log_file_path: '/var/log/mongodb-mms/monitoring-agent.log'
  config_dir_path: '/etc/mongodb-mms'
  log_dir_path: '/var/log/mongodb-mms'
  process_name: 'mongodb-mms-monitoring-agent'
  process_user: 'mongodb-mms-agent'
  process_group: 'mongodb-mms-agent'

# Version 4 format
  packages:
    main:
      name: mongodb-mms-monitoring-agent
  services:
    mongodb-mms-monitoring-agent:
      process_name: mongodb-mms-monitoring-agent
      process_user: mongodb-mms-agent
      process_group: mongodb-mms-agent
  files:
    config:
      path: "/etc/mongodb-mms/monitoring-agent.config"
    log:
      path: "/var/log/mongodb-mms/monitoring-agent.log"
    pid:
      path: "/var/run/mongodb-mms-monitoring-agent.pid"
  user_files:
    config:
      path: "$HOME/.mongodb-mms/monitoring-agent.config"
  dirs:
    config:
      path: "/etc/mongodb-mms"
    log:
      path: "/var/log/mongodb-mms"
  user_dirs:
    config:
      path: "$HOME/.mongodb-mms"
