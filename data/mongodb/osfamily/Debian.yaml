---
mongodb::settings:
  pid_file_path: '/var/run/mongodb/mongodb.pid'
  init_file_path: '/etc/default/mongodb'

# Version 4 format
  files:
    init:
      path: "/etc/default/mongodb"
    pid:
      path: "/var/run/mongodb/mongodb.pid"
