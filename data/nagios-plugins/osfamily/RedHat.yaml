---
nagios-plugins::settings:
  package_name: 'nagios-plugins-all'
  tp_prerequisites:
    - epel

# Version 4 format
  packages:
    main:
      name: nagios-plugins-all
  preinstall:
    tp::install:
    - epel
