---
nats-server::settings:
  choco_package_name: 'nats-server'
  package_name: 'nats-server'
  install_method: package
# Version 4 format
  packages:
    main:
      name: nats-server
      providers:
        choco:
          name: nats-server
