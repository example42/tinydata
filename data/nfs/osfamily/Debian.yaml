---
nfs::settings:
  package_name: 'nfs-kernel-server'
  service_name: 'nfs-kernel-server'
  init_file_path: '/etc/default/nfs'

# Version 4 format
  packages:
    main:
      name: nfs-kernel-server
  services:
    nfs-kernel-server: {}
  files:
    init:
      path: "/etc/default/nfs"
