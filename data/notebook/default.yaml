---
notebook::settings:
  package_name: notebook
  package_provider: 'pip'


# No data to convert to Version 4 format

# Version 4 format
  packages:
    main:
      name: notebook
      provider: pip
