---
openssh::settings:
  package_name: 'openssh'
  service_name: 'sshd'
  config_file_mode: '0640'
  init_file_path: '/etc/sysconfig/sshd'

# Version 4 format
  packages:
    main:
      name: openssh
  services:
    sshd: {}
  files:
    init:
      path: "/etc/sysconfig/sshd"
