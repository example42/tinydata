---
passenger-apache::settings:
  repo_file_url: 'https://oss-binaries.phusionpassenger.com/yum/definitions/el-passenger.repo'
  repo_file_name: 'passenger.repo'
  package_name: 'mod_passenger'
  tp_prerequisites:
    - epel
  package_prerequisites:
    - pygpgme

# Version 4 format
  packages:
    main:
      name: mod_passenger
  repo:
    upstream:
      repofile_url: https://oss-binaries.phusionpassenger.com/yum/definitions/el-passenger.repo
  preinstall:
    tp::install:
    - epel
    package:
    - pygpgme
