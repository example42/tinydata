---
pgbouncer::settings:
  repo_url: 'http://apt.postgresql.org/pub/repos/apt'
  apt_release: 'buster-pgdg main'

# Version 4 format
  repo:
    upstream:
      url: http://apt.postgresql.org/pub/repos/apt
      apt:
        release: buster-pgdg main
