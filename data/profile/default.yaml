---
profile::settings:
  config_dir_path: '/etc/profile.d'
  config_file_path: '/etc/profile'
  conf_dir_path: '/etc/profile.d'

# Version 4 format
  files:
    config:
      path: "/etc/profile"
  user_files:
    config:
      path: "$HOME/.profile"
  dirs:
    config:
      path: "/etc/profile.d"
    conf:
      path: "/etc/profile.d"
  user_dirs:
    config:
      path: "$HOME/.profile.d"
