---
prometheus::settings:
  brew_package_name: 'prometheus'
  user_config_file_path: '$HOME/Library/Application\ Support/Prometheus/prometheus.yml'
  user_config_dir_path: '$HOME/Library/Application\ Support/Prometheus/'

  user_files:
    config:
      path: '$HOME/Library/Application\ Support/Prometheus/prometheus.yml'

  user_dirs:
    config:
      path: '$HOME/Library/Application\ Support/Prometheus/prometheus.yml'
# Version 4 format
  packages:
    main:
      providers:
        brew:
          name: prometheus
