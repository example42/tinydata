---
sendmail::settings:
  package_name: 'sendmail-bin'
  log_file_path: '/var/log/mail.log'
  init_file_path: '/etc/default/sendmail'

# Version 4 format
  packages:
    main:
      name: sendmail-bin
  files:
    log:
      path: "/var/log/mail.log"
    init:
      path: "/etc/default/sendmail"
