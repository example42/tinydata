---
sendmail::settings:
  package_name: 'sendmail-cf'
  init_file_path: '/etc/sysconfig/sendmail'

# Version 4 format
  packages:
    main:
      name: sendmail-cf
  files:
    init:
      path: "/etc/sysconfig/sendmail"
