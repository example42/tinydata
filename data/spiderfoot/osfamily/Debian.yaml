---
spiderfoot::settings:
  init_file_path: '/etc/default/spiderfoot'

  files:
    init:
      path: '/etc/default/spiderfoot'

  upstream_repo: true
# Version 4 format
  files:
    init:
      path: "/etc/default/spiderfoot"
  repo: upstream
