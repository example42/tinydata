---
supervisor::settings:
  config_file_path: '/etc/supervisord.conf'
  config_dir_path: ''
  conf_dir_path: ''

# Version 4 format
  files:
    config:
      path: "/etc/supervisord.conf"
  user_files:
    config:
      path: "$HOME/.supervisord.conf"
  dirs:
    config:
      path: ''
    conf:
      path: ''
  user_dirs:
    config:
      path: ''
