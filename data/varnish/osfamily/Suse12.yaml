---
varnish::settings:
  zypper_repofile_url: 'http://download.opensuse.org/repositories/server:/http/SLE_12/server:http.repo'
  repo_name: 'server_http'
  init_file_path: '/etc/sysconfig/varnish'
  config_file_path: '/etc/varnish/vcl.conf'
  pid_file_path: '/var/run/varnishd.pid'

# Version 4 format
  files:
    config:
      path: "/etc/varnish/vcl.conf"
    init:
      path: "/etc/sysconfig/varnish"
    pid:
      path: "/var/run/varnishd.pid"
  user_files:
    config:
      path: "$HOME/.varnish/vcl.conf"
  repo:
    upstream:
      name: server_http
      zypper_repofile_url: http://download.opensuse.org/repositories/server:/http/SLE_12/server:http.repo
